import { Category } from './category.model';

export class Product {
    id: string;
    productName: string;
    description: string;
    characteristics: string;
    price: number;
    categoryId: string;
  }
  